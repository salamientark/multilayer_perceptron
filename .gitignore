# Python
**/__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/

# Coverage / test artifacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/

# Program output (generated by the pipeline, not source).
# Anchored to the repo root so the tests/test_data/ fixtures stay tracked.
/data_training.csv
/data_validation.csv
/prediction.csv
/weights.npz
/weights.csv
/trained_model.json
/save/**

# Plots
**/*.png
